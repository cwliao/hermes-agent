# HERMES-TELEGRAM-DELIVERY-VERIFICATION-001 implementation review

Review the current working tree against base commit
`20aadf71cb4ead7db7d9590310465e90c937558b`.

This is one identical, metadata-only correction packet for exactly one
authenticated Claude reviewer and one authenticated AGY reviewer. Review only;
do not edit, commit, push, merge, deploy, restart DGX, or modify Telegram state.
Do not print or request message bodies, tokens, credentials, raw chat/user
identifiers, or sensitive absolute paths.

## Requested change

Make one Telegram inbound event correlate to its outbound response using an
opaque correlation id, with redacted audit records that are safe for runtime
verification. Preserve existing delivery behavior and platform routing.

## Expected evidence contract

- Processable Telegram inbound events emit a metadata-only audit record with
  `phase=inbound`, `status=accepted`, `correlation_id`, and `update_id`.
- Correlated Telegram sends emit a metadata-only audit record with
  `phase=outbound`, `status=delivered` or `failed`, `correlation_id`, and safe
  result metadata.
- Native voice, image, document, video, animation, and media-group paths that
  receive correlated metadata also emit outbound audit evidence or delegate to
  a path that does.
- Native-media fallback paths emit exactly one outbound audit record because
  the base fallback's dynamic `self.send()` is already audited.
- Text, photo-burst, and media-group batching preserves every accepted input
  correlation id and emits a matching outbound audit record for each id.
- Event correlation metadata reaches all existing base outbound response paths,
  including active-session command, clarify, normal background, and error
  notification paths.
- No message body, token, credential, raw chat/user identity, or sensitive
  absolute path is added to the audit record.
- Existing send results and retry behavior remain unchanged.

## Changed files in scope

- `gateway/platforms/base.py`
- `plugins/platforms/telegram/adapter.py`
- `tests/gateway/test_telegram_delivery_audit.py`
- `docs/plans/2026-08-17-hermes-telegram-delivery-verification-001.md`

## Verification already run

- `tests/gateway/test_telegram_delivery_audit.py`
- `tests/gateway/test_telegram_text_batching.py`
- `tests/gateway/test_telegram_final_delivery.py`
- Result: `38 passed` in the isolated worktree's WSL Python environment.
- Ruff check passed for all changed Python files.
- `git diff --check` passed.
- The same 38-test command was independently rerun from the reviewer-facing
  WSL environment after materializing only required tracked source; no test or
  implementation files changed during that verification.

## Reviewer response

Return exactly one verdict: `PASS`, `REVISE`, or `BLOCKED`, followed by
metadata-only findings tied to file/line and the acceptance contract. Treat
missing coverage, privacy leakage, behavior changes, or an inability to inspect
the current working tree as a non-PASS result.
