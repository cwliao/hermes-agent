# CI-BASELINE-001: Restore the blocking Python CI baseline

**Status:** IMPLEMENTED_PENDING_CI; local validation is complete and remote
GitHub Actions verification is still required.

**Priority:** P1 - `main` is usable and DGX is healthy, but the required-check
gate is red and forced the ARCH-001 owner-authorized admin merge.

## Evidence

- Mainline commit: `5fb3d5cd1ee8358570e86d9fbce95e8e73dd584b`.
- Reproduced in CI runs
  [`31352376845`](https://github.com/cwliao/hermes-agent/actions/runs/31352376845)
  and
  [`31352008188`](https://github.com/cwliao/hermes-agent/actions/runs/31352008188).
- Lint, OSV scan, attribution, uv lock, Docker-script checks, and JS/TS checks
  passed in both runs.

## Failure inventory

1. `tests/agent/test_codex_responses_adapter.py::test_normalize_codex_response_salvage_is_xai_scoped`
   reports `stop` where the test expects `incomplete`.
2. `tests/gateway/test_unauthorized_dm_behavior.py::test_handle_message_drops_anonymous_sender_outside_allowlist`
   reports that the authorization gate did not drop the message.
3. `tests/hermes_cli/test_models.py::TestFetchOpenRouterModels::test_live_fetch_recomputes_free_tags`
   has a model-list/free-tag expectation mismatch.
4. `tests/hermes_cli/test_models.py::TestFetchOpenRouterModels::test_permissive_when_supported_parameters_missing`
   cannot find the expected `qwen/qwen3.7-max` entry.
5. `tests/hermes_cli/test_startup_plugin_gating.py::test_builtin_set_covers_every_registered_subcommand`
   reports missing builtin registration for `smartocr`.

## Objective

Restore the required Python CI gate with focused fixes that preserve current
Hermes behavior and ARCH-001 runtime-state integration.

## Scope

- Reproduce all five failures locally from the clean `main` baseline.
- Identify whether each failure is a code regression, stale fixture/contract,
  or upstream model-catalog drift before changing tests.
- Fix the smallest production or test-fixture surface that matches the actual
  contract.
- Add or update focused regression coverage where the behavior is intentional.
- Run the affected tests, corresponding CI slices, and all blocking static
  checks.

## Non-goals and safety boundaries

- Do not weaken, skip, quarantine, or broadly rewrite failing tests merely to
  make CI green.
- Do not change ARCH-001 runtime-state behavior unless a failure proves a direct
  regression.
- Do not edit, reset, or restart the live DGX checkout while implementing this
  ticket.
- Do not touch Claude-owned dirty/diverged worktrees.
- No merge or deployment until the review loop reaches consensus.

## Acceptance criteria

- All five named failures pass locally and in GitHub Actions.
- The relevant Python slices and `All required checks pass` are green.
- Ruff, OSV, attribution, uv-lock, Docker-script, and JS/TS checks remain green.
- Each fix has a concise cause/evidence note; no test is silenced without an
  approved contract change.
- ARCH-001 targeted tests remain green.
- A rollback-ready commit and independent review record exist before merge.

## Review record

- Codex initial review: **READY WITH CONDITIONS** - failure inventory and
  boundaries are specific; the formatting artifact found during cross-review
  was removed, and the code-versus-catalog distinction remains required.
- Independent cross-review: **READY** (AGY text-only review; no remaining
  content findings).
- Consensus: **READY FOR IMPLEMENTATION**.
- Current blocker: none for implementation; merge and deployment remain later
  gates requiring their own evidence.

## Implementation evidence

- Codex adapter test now supplies the explicit `codex_backend` issuer contract;
  production normalization remains issuer-scoped.
- Gateway pending OCR/last-30-days state is now inspected only after the inbound
  authorization gate, preventing unauthorized session-key access.
- OpenRouter model tests pin the in-repo fallback snapshot instead of depending
  on the live curated manifest, while still testing the live API response
  transformation.
- `smartocr` is now included in `_BUILTIN_SUBCOMMANDS`.
- Targeted failures: **5 passed**.
- Full affected modules: **184 passed** (26 adapter, 37 authorization, 84
  model, 37 startup-gating).
- ARCH-001 regression set: **35 passed**.
- Ruff, compileall, and `git diff --check`: passed.
- Live DGX was not modified or restarted.
